name: Tests

on:
  push:
    branches:
      - main
    paths:
      - "*.yml"
      - ".github/workflows/*.yml"
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    branches:
      - main
    paths:
      - "*.yml"
      - ".github/workflows/*.yml"

jobs:
  test:
    strategy:
      matrix:
        include:
          - SDL_VERSION: 2.32.10
            SDL_IMAGE_VERSION: 2.8.12
            SDL_TTF_VERSION: 2.24.0
            SDL_MIXER_VERSION: 2.8.2
          - SDL_VERSION: 3.4.8
            SDL_IMAGE_VERSION: 3.4.4
            SDL_TTF_VERSION: 3.2.2
            SDL_MIXER_VERSION: 3.2.2

    runs-on: ubuntu-latest
    if: github.event.pull_request.draft == false

    steps:
      - name: Check out repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Install SDL dependencies
        uses: ./
        with:
          SDL_VERSION: ${{ matrix.SDL_VERSION }}
          SDL_IMAGE_VERSION: ${{ matrix.SDL_IMAGE_VERSION }}
          SDL_TTF_VERSION: ${{ matrix.SDL_TTF_VERSION }}
          SDL_MIXER_VERSION: ${{ matrix.SDL_MIXER_VERSION }}

      - name: Test directories exist
        run: |
          ls /tmp/.sdl/
          ls /tmp/.sdl/SDL-${{ matrix.SDL_VERSION }}/
          ls /tmp/.sdl/SDL_image-${{ matrix.SDL_IMAGE_VERSION }}/
          ls /tmp/.sdl/SDL_ttf-${{ matrix.SDL_TTF_VERSION }}/
          ls /tmp/.sdl/SDL_mixer-${{ matrix.SDL_MIXER_VERSION }}/
